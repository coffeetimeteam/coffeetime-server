spring:
  profiles:
    group:
      dev: "dev"
      prod: "prod"
    include:
      - security
  devtools:
    restart:
      enabled: true
      additional-exclude: static/**,public/**
    livereload:
      enabled: true

logging:
  discord:
    webhook-url: ${DISCORD_WEBHOOK}